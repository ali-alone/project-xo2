<UserControl x:Class="project_xo2.View.Main"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:project_xo2.ViewModel"
        xmlns:local="clr-namespace:project_xo2.View"
        xmlns:controls="clr-namespace:clockcontrol;assembly=clockcontrol"
        mc:Ignorable="d"
                Style="{StaticResource Page_Style}"
  d:DesignHeight="600" d:DesignWidth="622">
    <UserControl.DataContext>
        <vm:MainWM/>
    </UserControl.DataContext>
    <Grid>
        <Border>
            <Border.Background>
                <ImageBrush  ImageSource="/image/home.png" Stretch="Fill"/>
            </Border.Background>
        </Border>
        <Border>
            <controls:UserControl1 x:Name="ClockControl"
           HorizontalAlignment="Left" VerticalAlignment="Top"
           Margin="412,77,0,0" Width="200" Height="50" Background="Transparent" Foreground="#395a78"/>
        </Border>
    </Grid>
</UserControl>
